// #include "Database.h"
// #include <stdexcept>
// #include <iostream>

// Database :: Database(string host, string user, string password, string dbName)
//     : host(host), user(user), password(password), dbName(dbName) {
//     conn = mysql_init(NULL);
// }